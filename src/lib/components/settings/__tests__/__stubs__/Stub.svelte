<!--
  Minimal Svelte component stub for SettingsPage deep-link tests. Used to
  replace the heavy settings subcomponents (IntegrationsSettings,
  AiSettings, UpdateSettings, …) whose real implementations pull in
  Tauri IPC + OS detection.
-->
<div data-testid="settings-stub"></div>
